# Project-Owned EPUB Fixture

The files in this directory are synthetic project-owned inputs covered by the
repository license. Tests package them into a deterministic EPUB container.
They intentionally exercise only the documented S-042A subset: EPUB 3 metadata,
two UTF-8 XHTML spine items, and `h1`/`h2` headings.
